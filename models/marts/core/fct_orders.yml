- name: fct_orders
  columns:
    - name: customer_id
      tests:
        - relationships:
            to: ref('stg_customers')
            field: id
  tests:
  - accepted_statuses:
      column_name: status